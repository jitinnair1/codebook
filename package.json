{
  "name": "sowar",
  "version": "0.1.0",
  "description": "in-browser OCaml exercises",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "check": "tsgo --noEmit",
    "build:dist": "mkdir -p dist && cp -r public/* dist/",
    "build:ts": "bun scripts/build.ts",
    "build:css": "bunx --bun @tailwindcss/cli -i ./src/input.css -o ./dist/style.css --minify",
    "build:html": "bun scripts/inject-meta.ts",
    "build": "bun run clean && bun run check && bun run build:dist && bun run build:ts && bun run build:css && bun run build:html",
    "dev": "bun run dev.ts",
    "serve": "bun server.ts"
  },
  "dependencies": {
    "@catppuccin/codemirror": "^1.0.3",
    "@codemirror/autocomplete": "^6.20.2",
    "@codemirror/commands": "^6.10.3",
    "@codemirror/language": "^6.12.3",
    "@codemirror/legacy-modes": "^6.5.3",
    "@codemirror/lint": "^6.9.6",
    "@codemirror/search": "^6.7.0",
    "@codemirror/state": "^6.6.0",
    "@codemirror/theme-one-dark": "^6.1.3",
    "@codemirror/view": "^6.43.0",
    "@tailwindcss/cli": "^4.3.0",
    "@types/canvas-confetti": "^1.9.0",
    "canvas-confetti": "^1.9.4",
    "codemirror": "^6.0.2",
    "marked": "^17.0.6",
    "zustand": "^5.0.13"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.19",
    "@typescript/native-preview": "^7.0.0-dev.20260521.1",
    "bun-types": "^1.3.14",
    "tailwindcss": "^4.3.0"
  }
}